Starting Container
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
configuration rejected - settings: Value error, LOCAL_DATABASE_URL is set in a Railway environment. Serving never touches local Postgres (SPEC.md section 2.1), and a laptop's Postgres is unreachable from Railway regardless - so this variable can only mislead. Delete it from the Railway service's variables.
